\documentclass[11pt]{article}
\input{../calcnotes.sty}
\fancyhead[C]{1.2 Functions and Graphs}

\begin{document}

    \noindent The values of one variable often depend on another. We call this a function. We define a function as a rule that assigns a \underline{\hspace{2cm}} element of the range ($R$) to each element in the domain ($D$). Counter-examples:\\[2ex]

    \noindent Since the purpose of mathematics is to model the world, a function is pretty important. We write a function as:\\[2ex]

    \noindent What is the function that expresses the area of a circle?\\[2ex]

    \noindent Domain is the \underline{\hspace{2cm}}. Range is the \underline{\hspace{2cm}}. What are the domain and range for the circle function?\\[2ex]

    \noindent What is interval notation? Show $\displaystyle -3 < x \le 6$ with interval notation (and any other way you can think of). What about for the circle above?

    \noindent Identify the domain and range of the following graphs. Use interval notation.\\[1ex]

    \begin{align*}
        y=\frac{1}{x} &  & y=\sqrt{x}
    \end{align*}

    \vspace{1ex}

    \begin{align*}
        y=\sqrt{4-x^2} &  & y=x^\frac{2}{3}
    \end{align*}\\[2ex]

    \noindent What is the definition of an even function? An odd function?\\[2ex]

    \noindent How do we know if a function is even or odd by looking at its graph?\\[2ex]

    \noindent Determine if the following functions are odd, even, or neither.

    \begin{align*}
        f(x) = x^2 &  & f(x) = x^2 + 1 &  & f(x) = x &  & f(x) = x + 1
    \end{align*}

    \vspace{2ex}

    \newpage

    \noindent How do we graph a piecewise function? Graph and find the domain and range:\\[1ex]

    \begin{minipage}[b]{0.5\textwidth}
        \[f(x) =
            \begin{cases}
                -x,  & x < 0         \\
                x^2, & 0 \le x \le 1 \\
                1,   & x > 1
            \end{cases}
        \]

        \vspace{1ex}
    \end{minipage}
    \begin{minipage}[c]{0.5\textwidth}
        \centering
        \emptygraph
    \end{minipage}\\[2ex]

    \noindent Graph the following: $\displaystyle f(x) = |x - 2| + 3$. Then write it as a piecewise function. Also find the domain and range.\\[1ex]

    \emptygraph

    \vspace{2ex}

    \noindent Finally, what is a composite function? If $\displaystyle f(x) = x - 7$ and $\displaystyle g(x) = x^2$, what is $\displaystyle f(g(x))$? What is $\displaystyle (f \circ g)(x)$? What is $\displaystyle g(f(2))$?\\[2ex]

    \noindent Composite functions are used a lot in Calculus.\\[2ex]

\end{document}
